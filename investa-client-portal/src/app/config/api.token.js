import { InjectionToken } from '@angular/core';
/**
 * Injection token for the backend API base URL.
 * Use this token in services to get a configurable API base.
 */
export const API_BASE = new InjectionToken('API_BASE');
