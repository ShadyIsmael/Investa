# TODO

## Admin navbar rebuild (compile first)
- [x] Inspect `admin-navbar.component.ts` and `admin-navbar.component.scss` to understand required template API.
- [ ] Replace `src/app/components/admin-navbar/admin-navbar.component.html` with clean minimal template (nav/div container only, shallow nesting).
- [ ] Validate that exactly one `<nav>` and one `</nav>` exist.
- [ ] Validate all Angular blocks `@if/@else/@for/@switch` are correctly structured.
- [ ] Run `npm run build` for `investa-client-portal`.
- [ ] Record TypeScript error count.
- [ ] Produce final output summary + build result.

